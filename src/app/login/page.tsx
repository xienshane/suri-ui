import SampleCard from "@/components/SampleCard";

export default function LoginPage() {
  return (
    <main className="p-10">
      <SampleCard
        title="Login"
        description="Sample page for the login route."
      />
    </main>
  );
}
